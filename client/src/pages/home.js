import React from "react";
import GameBoard from "../components/gameComponents/GameBoard";

const Home = () => {
  return (
    <section>
      <GameBoard/>
    </section>
  );
}

export default Home;
